# Application
APP_ENV=development
PORT=8080
LOG_LEVEL=info

# Redis (required) — asynq job queue backend
# Format: host:port
REDIS_ADDR=localhost:6379

# Kafka (required) — comma-separated list of broker host:port pairs
KAFKA_BROKERS=localhost:9092
