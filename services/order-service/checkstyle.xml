<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Project-tuned Checkstyle configuration.
  Uses 4-space indentation and does not require Javadoc on all members.
  Enforces import hygiene and standard Java naming conventions.
-->
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="fileExtensions" value="java"/>

    <module name="TreeWalker">

        <!-- ── Imports ── -->
        <module name="AvoidStarImport"/>
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>
        <module name="IllegalImport"/>

        <!-- ── Naming conventions (Java standard) ── -->
        <module name="TypeName"/>
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
        </module>
        <!--
          Allow the conventional lowercase SLF4J logger name "log" / "logger"
          in addition to the standard UPPER_SNAKE_CASE constant pattern.
        -->
        <module name="ConstantName">
            <property name="format"
                value="^log(ger)?$|^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$"/>
        </module>
        <module name="MemberName"/>
        <module name="ParameterName"/>
        <module name="LocalVariableName"/>
        <module name="MethodName"/>

        <!-- ── Code structure ── -->
        <module name="EmptyCatchBlock">
            <property name="exceptionVariableName" value="expected|ignored"/>
        </module>
        <module name="FallThrough"/>
        <module name="MultipleVariableDeclarations"/>

        <!-- ── Miscellaneous quality checks ── -->
        <module name="UpperEll"/>
        <module name="ArrayTypeStyle"/>
        <module name="ModifierOrder"/>

    </module>
</module>
