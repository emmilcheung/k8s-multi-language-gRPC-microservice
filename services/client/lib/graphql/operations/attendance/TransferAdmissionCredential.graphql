mutation TransferAdmissionCredential($input: TransferAdmissionCredentialInput!) {
  transferAdmissionCredential(input: $input) {
    pendingTransferId
    credential {
      id
      transferState
      transferredTo
      transferredAt
    }
  }
}
