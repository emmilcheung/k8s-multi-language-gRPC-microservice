query TicketsBrowse($first: Int, $after: String) {
  ticketsConnection(filter: { availableOnly: true }, first: $first, after: $after) {
    edges {
      node {
        id
        title
        price
        available
        ticketType
        seatingPlan {
          id
        }
        event {
          title
          startsAt
          venueName
        }
      }
      cursor
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
