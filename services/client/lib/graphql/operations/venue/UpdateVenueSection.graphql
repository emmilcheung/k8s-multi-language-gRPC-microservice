mutation UpdateVenueSection($id: ID!, $input: UpdateSectionInput!) {
  updateSection(id: $id, input: $input) {
    id
    venueId
    name
    type
    rowCount
    columnCount
    displayOrder
    capacity
  }
}
