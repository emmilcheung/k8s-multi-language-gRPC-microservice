{
  "name": "@ticketing/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for the ticketing platform — agent-native API access via OAuth2",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ticketing-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "node --import tsx/esm src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "open": "^10.1.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.7.0",
    "typescript": "^7.0.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
