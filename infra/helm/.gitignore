# Downloaded external chart tarballs (Bitnami, Kong, etc.) — rebuilt by CI
charts/*.tgz

# Local application sub-chart tarballs (also rebuilt by CI)
charts/auth-service-*.tgz
charts/client-*.tgz
charts/cp-kafka-*.tgz
charts/expiration-service-*.tgz
charts/order-service-*.tgz
charts/payment-service-*.tgz
charts/ticket-service-*.tgz

# Packaged root-level charts
*.tgz

# Dependency lock file (optional, but often regenerated by CI)
# Chart.lock

# IDE/Editor files (examples)
.idea/
.vscode/
*.iml
.DS_Store
temp/
target/

# OS and build artifacts
/bin
build/
dist/

# Sensitive or user-specific files
*.env
*.secret
credentials.json
